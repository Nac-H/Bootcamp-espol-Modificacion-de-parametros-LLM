import { createConfigFromEnv } from "../types/config.js";
const getAppConfig = () => {
    const config = createConfigFromEnv();
    return config;
};
export default getAppConfig;
//# sourceMappingURL=index.js.map
